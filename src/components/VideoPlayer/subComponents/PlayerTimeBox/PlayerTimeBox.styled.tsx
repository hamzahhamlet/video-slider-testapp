import { styled, Box, Typography } from "@mui/material";

export const TimeBoxWrapper = styled(Box)({
  display: "flex",
  alignItems: "center",
});

export const TinyText = styled(Typography)({
  fontSize: "0.75rem",
  fontWeight: 500,
  letterSpacing: 0.2,
  color: "#fff",
});
